import React from 'react';
import {AppBar, Toolbar} from '@material-ui/core';


function TopBar(){

    return(
        <AppBar >
            <Toolbar>

            </Toolbar>
        </AppBar>
    )
}

export default TopBar;